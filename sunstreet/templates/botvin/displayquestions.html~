<!DOCTYPE html>
<html>	
	<head>
		<title>{{school_level}} Botvin Lifeskills Survey {{section}}</title>
		<meta charset = "utf-8" />
		<link rel="stylesheet" type="text/css"href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			320px)" href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			480px)" href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			1024px)" href="/static/lifeskills/smalldevice.css" />

	</head>
	<body class = "body">
		<header class = "mainHead">
			<h1> Botvin Lifeskills Section {{section}}</h1>
		</header>

		{% if error_message %}
			<h3><strong>{{ error_message }}</strong></h3>
			<a href="{% url 'botvin_lifeskills:botvinsurvey' {{section}} {{school_level}} %}"><h4>GO BACK</h4></a>
		{% endif %}

        {% ifequal section "B" %}<h1> Read each statement below and indicate whether you think each is True or False by filling in the appropriate circle. </h1>{% endifequal %}

        {% if school_level == "ES" %}{% if section == "C" %}<h1> Mark the circle to show if you agree or disagree with each of the statements below. </h1>{% endif %}
        {% elif section == "C" %}<h1> Please fill in the circle to show how much you agree or disagree with each statement.</h1>{% endif %}

        {% if school_level == "ES" %}{% if section == "D" %}<h1> Mark he circle for each statement to show how often you do the following things</h1>{% endif %}
        {% elif section == "D" %}<h1> Please fill in each circle to show how you would handle the following situations. </h1>{% endif %}

        <form action="{% url 'botvin_lifeskills:botvinSectionVote'  %}" method="post">

            <input type="hidden" value="{{school_level}}" name="school_level">
            <input type="hidden" value="{{section}}" name="section">
		{% csrf_token %}

		{% for question in questions %}
                {% if school_level == "ES" %}{% if section == "D" %}{% if forloop.counter == 7 %}<h2> Mark the word for each statement to show how likely you would be to do the following things.</h2>{%endif%}{%endif%}
                {% elif section == "D" %}{% if forloop.counter == 1 %}<h2> How likely would you be to: </h2>{%endif%}
                {% if forloop.counter == 6 %}<h2> How likely would you do the following things: </h2>{%endif%}
                {% if forloop.counter == 9 %}<h2> When you feel anxious you: </h2>{%endif%}
                {% if forloop.counter == 11 %}<h2> In general: </h2>{%endif%}{%endif%}

            <p>{{question.question_number}}. {{question}}</p>
		    {% for choice in question.answer_set.all %}

                <h5></h5>
			    <input type="radio" name=choice{{question.question_number}} id="choice{{ forloop.counter }}" value="{{ choice.id }}" />

			    <label for="choice{{ forloop.counter }}">{{ choice.choices }}</label><br />
		    {% endfor %}
        {% endfor %}

		{% if not error_message %}
			<div class="buttonHolder">
				<input type="submit" value="Submit" />
			</div>
		{% endif %}
			
		</form>
	
	</body>
</html>
