<!DOCTYPE html>
<html>
	<head>
		<title>Life Skills Post Survey</title>
		<meta charset = "utf-8" />
		<link rel="stylesheet" type="text/css"href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			320px)" href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			480px)" href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			1024px)" href="/static/lifeskills/smalldevice.css" />

		
	</head>
	<body class = "body">
		<header class = "mainHead">
			<h1> Life Skills Trainings Post Survey </h1>
		</header>

		{% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}

	   	<form action="{% url 'lifeskills:posttestvote' %}" method="post">

		{% csrf_token %}

		
		
		<p>{{question_1}}</p>

		<textarea rows="4" cols="50" name="textarea1"></textarea>

		<p>{{question_2}}</p>

		<textarea rows="4" cols="50" name="textarea2"></textarea>
		
		<p>{{question_3}}</p>
		{% for choice in question_3.posttestanswer_set.all %}
			<input type="radio" name="choice3" id="choice{{ forloop.counter }}" value="{{ choice.id }}" />
			<label for="choice{{ forloop.counter }}">{{ choice.choices }}</label><br />
		{% endfor %}
		<p>{{question_4}}</p>
		{% for choice in question_4.posttestanswer_set.all %}
			<input type="radio" name="choice4" id="choice{{ forloop.counter }}" value="{{ choice.id }}" />
			<label for="choice{{ forloop.counter }}">{{ choice.choices }}</label><br />
		{% endfor %}
		<p>{{question_5}}</p>
		{% for choice in question_5.posttestanswer_set.all %}
			<input type="radio" name="choice5" id="choice{{ forloop.counter }}" value="{{ choice.id }}" />
			<label for="choice{{ forloop.counter }}">{{ choice.choices }}</label><br />
		{% endfor %}
		<p>{{question_6}}</p>
		{% for choice in question_6.posttestanswer_set.all %}
			<input type="radio" name="choice6" id="choice{{ forloop.counter }}" value="{{ choice.id }}" />
			<label for="choice{{ forloop.counter }}">{{ choice.choices }}</label><br />
		{% endfor %}
		<p>{{question_7}}</p>
		{% for choice in question_7.posttestanswer_set.all %}
			<input type="radio" name="choice7" id="choice{{ forloop.counter }}" value="{{ choice.id }}" />
			<label for="choice{{ forloop.counter }}">{{ choice.choices }}</label><br />
		{% endfor %}
		<p>{{question_8}}</p>
		{% for choice in question_8.posttestanswer_set.all %}
			<input type="radio" name="choice8" id="choice{{ forloop.counter }}" value="{{ choice.id }}" />
			<label for="choice{{ forloop.counter }}">{{ choice.choices }}</label><br />
		{% endfor %}
		
		<div class="buttonHolder">
			<input type="submit" value="Submit" />
		</div>
		</form>
	
	</body>
</html>
