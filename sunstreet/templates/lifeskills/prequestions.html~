<!DOCTYPE html>
<html>
	<head>
		<title>Life Skills Survey</title>
		<meta charset = "utf-8" />
		<link rel="stylesheet" type="text/css"href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			320px)" href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			480px)" href="/static/lifeskills/smalldevice.css" />
		<link rel="stylesheet" type="text/css" media="only screen and (max-device-width: 			1024px)" href="/static/lifeskills/smalldevice.css" />
		<link rel="shortcut icon" href="/static/img/Sunstreet.jpg">
		
	</head>
	<body class = "body">
		<header class = "mainHead">
			<h1> Life Skills Trainings PreSurvey </h1>
		</header>
		
		<form action="{% url 'lifeskills:pretestvote' %}" method="post">
		{% if error_message %}<p><strong>{{ error_message }}</strong></p>{% endif %}

	   

		{% csrf_token %}

		<p>{{first_question}}</p>

		{% for choice in first_question.pretestanswer_set.all %}
			<input type="radio" name="choice1" id="choice{{ forloop.counter }}" value="{{ choice.id }}" />
			<label for="choice{{ forloop.counter }}">{{ choice.choices }}</label><br />
		{% endfor %}

		<p>{{second_question}}</p>
		
		{% for choice in second_question.pretestanswer_set.all %}
			<input type="radio" name="choice2" id="choice{{ forloop.counter }}" value="{{ choice.id }}" />
			<label for="choice{{ forloop.counter + 4 }}">{{ choice.choices }}</label><br />
		{% endfor %}

		<div class="buttonHolder">
			<input type="submit" value="Submit" />
		</div>
		</form>
	</body>
</html>
		
